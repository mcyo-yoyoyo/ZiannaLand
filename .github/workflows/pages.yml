name: Deploy Zianna Land to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Prepare static site
        run: |
          mkdir -p _site/content _site/icons _site/assets
          cp index.html app.css app.js manifest.webmanifest sw.js _site/
          cp content/program.js content/readers.js content/dialogues.js content/manifest.json _site/content/
          cp icons/icon.svg icons/icon-192.png icons/icon-512.png _site/icons/
          cp assets/zianna-land-characters-v2.png _site/assets/
          touch _site/.nojekyll
      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
